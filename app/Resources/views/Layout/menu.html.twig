{% block menu %}
	
<div class="navbar-header">
	<button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#navbar-menu">
	  <span class="icon-bar"></span>
	  <span class="icon-bar"></span>
	  <span class="icon-bar"></span>
	</button>
	<a class="navbar-brand" href="{{ path('fragoso_homepage') }}">
		<span class="glyphicon glyphicon-home"></span>
	</a>
</div>

<div class="collapse navbar-collapse" id="navbar-menu">		
	<ul class="nav navbar-nav">
		
		<li class=""><a href="{{ path('catalogue_home') }}" class="" title="document">Catalogue</a></li>
		
		<li class=""><a href="{{ path('client_home') }}" class="" title="document">Clients</a></li>
	
		{#<li class=""><a href="{{ path('lister_commande', {'commande_etat': 'en-cours'}) }}" class="" title="document">Commandes</a></li>#}
		<li class=""><a href="{{ path('commande_home') }}" class="" title="document">Commandes</a></li>
		
		{#
		<li class="dropdown">
			<a href="#" class="dropdown-toggle" data-toggle="dropdown" title="cartographie">Commandes</a>
			<ul class="dropdown-menu">
				<li><a href="{{path('creer_commande')}}">Nouvelle</a></li>
				<li><a href="{{path('commande_home')}}">Toutes</a></li>
				<li><a href="{{path('commande_encours')}}">En cours</a></li>
			</ul>
		</li>
		#}
		
		{#<li class=""><a href="#" class="" title="document">Factures</a></li>#}
		
	</ul>
	<ul class="nav navbar-nav navbar-right">
			<li class="dropdown">
				{% if not is_granted("IS_AUTHENTICATED_REMEMBERED") %}
					<li><a href="{{ path('fos_user_security_login') }}">Login</a></li>
				{% else %}
					<a href="#" class="dropdown-toggle" data-toggle="dropdown" title="My Account"><img src="{{ asset('pictures/img_avatar2.png') }}" class="w3-circle" style="height:25px;width:25px" alt="Avatar"></a>
					<ul class="dropdown-menu">
						<li><a href="{{ path('fos_user_security_logout') }}">Logout</a></li>
					</ul>
				{% endif %}
			</li>
			
	</ul>
</div>

{% endblock %}
